from django.contrib import admin
from django.urls import path
from main import views

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', views.home, name='home'),
    path('display/<str:playerTag>/', views.playerDetail, name='player_detail')
]
